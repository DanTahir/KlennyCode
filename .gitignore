# Dependencies
node_modules/
.pnp/
.pnp.js

# Bun
bun.lockb

# Build output / packaged app
agent/out/
agent/dist/
agent/dist-*/
agent/release/
agent/build/icons/*.ico
agent/build/icons/*.icns
agent/build/icons/*.png
!agent/build/icons/.gitkeep

# electron-builder
agent/*.exe
agent/*.dmg
agent/*.AppImage
agent/*.deb
agent/*.blockmap

# Env / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# Test / coverage
coverage/

# Klenny personal project prefs (not shared via git)
KLENNY.local.md

# .klenny/ (skills, custom subagents) is intentionally source-controlled. Plans, auto-memory
# notes, and the codebase search index now live outside the repo, in Klenny Code's application
# data directory — these entries just guard against stray leftovers from older versions.
.klenny/memory/
.klenny/plans/
.klenny/index/
