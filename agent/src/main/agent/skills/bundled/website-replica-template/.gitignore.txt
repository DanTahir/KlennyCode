node_modules/
.next/
# Production builds use a separate distDir so `next build` cannot clobber the
# manifests a running `next dev` is serving from. Plain `.next/` does not match it.
.next-build/
out/
*.tsbuildinfo
next-env.d.ts
.DS_Store
.env*

# Capture cache: large, re-downloadable, and not source.
# public/assets IS committed — it is what makes the replica self-hosted.
scrape/raw/
scrape/shots/
scrape/analysis/
scrape/viewport-report.json
scrape/compare-report.json
