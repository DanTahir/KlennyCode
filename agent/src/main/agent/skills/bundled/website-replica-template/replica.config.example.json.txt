{
  "_comment": [
    "Copy to replica.config.json and set targetUrl. Everything else is optional;",
    "the defaults in scripts/lib/config.mjs are used when a key is absent.",
    "This file is the ONLY place site-specific knowledge lives — the scripts",
    "themselves are entirely URL-agnostic."
  ],

  "targetUrl": "https://example.com/some/page",
  "slug": "examplecomsomepage",
  "port": 3300,

  "_settleMs": "ms to wait after load before capturing (raise for slow hero animations)",
  "settleMs": 3500,
  "_scrollSettleMs": "ms to pause at each scroll step (raise for lazy-loading sites)",
  "scrollSettleMs": 90,

  "_links": "'absolute' rewrites every nav link to the live site; 'preserve' keeps them as-is",
  "links": "absolute",

  "_codegen": "All optional. Add site-specific runtime-artifact classes here as you find them.",
  "codegen": {
    "keepClasses": [],
    "stripClasses": null,
    "dropSelectors": null,
    "splitMinChildren": 5,
    "splitMinSections": 3,
    "_skipStylesheets": [
      "Substrings of captured stylesheet hrefs to leave OUT of the rebuilt",
      "cascade. Last resort: normally every sheet must be emitted in order.",
      "Use only for a sheet the BROWSER itself could not parse — e.g. a site",
      "serving an uncompiled .scss source (// comments, $vars) at a .scss URL.",
      "Browsers CSS-error-recover past it; webpack's postcss hard-fails the",
      "build. Confirm a later valid sheet redefines the same rules first, or",
      "you are silently deleting real styles. Skipped sheets are recorded in",
      "manifest.json with status 'SKIPPED'."
    ],
    "skipStylesheets": []
  },

  "_viewports": "Override only if the site has unusual breakpoints.",
  "viewports": null,

  "_extraAssets": [
    "Absolute URLs to download that the capture browser never requested.",
    "Capture runs at ONE viewport, so a responsive <source media> / srcset",
    "variant for another breakpoint is never fetched — and 404s at exactly the",
    "viewport the harness audits. Especially needed when the site selects the",
    "source in JS (matchMedia) instead of letting the browser choose."
  ],
  "extraAssets": []
}
